
export interface cardType {
    icon: any,
    title: string,
    description: string,

};

export interface categoryType {
    title: string,
    imageUrl: string,
}

export interface productType {
    id: number,
    title: string,
    imageUrl: string,
    price: number,
    rating: number,
    quantity: number
};